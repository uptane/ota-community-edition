gzip on;
gzip_proxied any;
gzip_vary on;
gzip_buffers 16 8k;
# gzip_types
#   application/atom+xml
#   application/javascript
#   application/json
#   application/rdf+xml
#   application/rss+xml
#   application/x-javascript
#   application/xml
#   application/xml
#   font/opentype
#   font/truetype
#   image/svg+xml
#   text/css
#   text/javascript
#   text/plain
#   text/xml;
proxy_http_version 1.1;
proxy_set_header Connection 'upgrade';
client_body_buffer_size 128k;
proxy_connect_timeout   90;
proxy_send_timeout      90;
proxy_read_timeout      90;
proxy_buffers           32 4k;
server_name_in_redirect off;

server {
    listen ${SERVER_PORT};
    server_name ${SERVER_NAME};
    index index.html index.htm;
    # error_log  /var/log/nginx/error.log;
    # access_log /var/log/nginx/access.log;
    root /usr/share/nginx/html;
    # root /usr/share/nginx/html;
    # location / {
    #     try_files $uri $uri/ = 404;
    # }

    # location / {
    #     try_files $uri $uri/ /index.html;
    # }
    # location ^/api {
    #     proxy_pass http://${API_ENDPOINT_HOST}:${API_ENDPOINT_PORT}/api;
    # }
    
    ___PROXY_CONF___
    # location ${src} {
    #     proxy_pass http://${API_ENDPOINT_HOST}:${API_ENDPOINT_PORT}${dest};
    # }
}
