ats.campaigner = {
  database = {
    catalog = "campaigner_test"
  }

  http.client.deviceregistry.uri = "localhost"
  http.client.director.uri = "localhost"

  scheduler = {
    pollingTimeout = 1 seconds
    delay = 10 milliseconds
    batchSize = 10
  }
}
